---
import Carrusel from "@/components/index/Carrusel.astro";
import Layout from "../../layouts/Layout.astro";
import QueHacemos from "@/components/index/QueHacemos.astro";
---

<Layout title="Tanoni Agro">
  <main transition:animate="slide">
    <section
      class="h-[100svh] relative w-full bg-negociaciones-image md:bg-fixed bg-center bg-no-repeat bg-cover"
    >
      <div class="absolute inset-0 bg-black/70 backdrop-blur-xs z-10"></div>
      <div class="relative h-full w-4/5 flex mx-auto items-center z-20">
        <div class="md:w-1/2 flex flex-col gap-2 headline">
          <p class="prose prose-base md:prose-lg text-xl text-gray-400 italic">
            Conectando el campo con el mercado.
          </p>
          <h1 class="prose prose-neutral text-white prose-base text-clamp-sm font-black lato-normal">
            Soluciones integrales en corretaje de cereales
          </h1>
          <p class="prose prose-base md:prose-lg text-gray-300">
            En Tanoni Agro SRL, somos una empresa familiar comenzando en el
            corretaje de cereales, aceites y subproductos en el mercado
            agropecuario argentino. Nuestro compromiso es ofrecer soluciones
            integrales y personalizadas que potencien el éxito de
            nuestros clientes.
          </p>
        </div>
      </div>
    </section>
    <Carrusel/>
    <QueHacemos />
  </main>
</Layout>

<!-- Configura el script para que se ejecute en el cliente -->
<script is:inline>
  document.addEventListener("DOMContentLoaded", () => {
    ScrollReveal().reveal(".headline", {
      reset: false, // Permite que la animación se repita al volver a entrar en el viewport
      distance: "50px", // Ajusta la distancia del desplazamiento
      duration: 1500, // Ajusta la duración de la animación en milisegundos
      easing: "ease-in-out", // Ajusta el easing para suavizar el efecto
      origin: "bottom", // Ajusta la dirección de origen de la animación
      interval: 200, // Añade un pequeño retraso entre los elementos
      delay: 300,
    });
  });
</script>
