---
import Layout from "../../layouts/Layout.astro";
import { MapPin, MessageCircleMore, Phone, Mail } from "lucide-react";
---

<Layout title="Tanoni Agro - Contact us">
  <section class="w-full" transition:animate="slide">
    <div class="w-full h-[100px] flex items-center justify-center">
      <div class="w-4/5 flex justify-center items-center">
        <h2 class="text-xl font-bold text-gray-700">CONTACT US</h2>
        <hr class="flex-grow border-t border-gray-300 ml-4" />
      </div>
    </div>

    <article class="w-full h-auto py-8 my-5 bg-gray-50">
      <div class="w-full md:w-4/5 h-full flex flex-col md:flex-row mx-auto gap-10 md:gap-0 px-4 md:px-0">
        <div class="w-full h-full flex flex-col items-center justify-center gap-5">
          <h2 class="text-xl font-bold opacity-55">Geographical Location</h2>
          <span class="w-full bg-slate-300 h-[1px]"></span>
          <p class="flex gap-2">
            <MapPin /> Argentina, Rosario, Córdoba 1437 – Piso 5 - Oficina 7.
          </p>
          <iframe
            id="mapIframe"
            src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3348.2014695739504!2d-60.645315724608906!3d-32.94568987183984!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x95b7ab17b43846cf%3A0xc824e97fb7b87121!2sC%C3%B3rdoba%201437%2C%20S2000AWU%20Rosario%2C%20Santa%20Fe!5e0!3m2!1ses-419!2sar!4v1773414238797!5m2!1ses-419!2sar"
            class="w-full max-w-[600px] h-[300px] md:h-[450px] rounded-lg"
            width="600"
            height="450"
            style="border:0;"
            allowfullscreen=""
            loading="lazy"
            referrerpolicy="no-referrer-when-downgrade"
          >
          </iframe>
        </div>
      </div>
    </article>
  </section>
</Layout>

<script>
  function adjustIframeSize() {
    const iframe = document.getElementById('mapIframe');
    const screenWidth = window.innerWidth;

    if (screenWidth < 768) {
      // Para pantallas móviles
      iframe.style.width = '100%';
      iframe.style.height = '300px';
    } else {
      // Para pantallas más grandes (desktop)
      iframe.style.width = '600px';
      iframe.style.height = '450px';
    }
  }

  // Llama a la función cuando cargue la página
  window.addEventListener('load', adjustIframeSize);

  // Vuelve a llamar a la función cuando cambie el tamaño de la ventana
  window.addEventListener('resize', adjustIframeSize);
</script>
